fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
numpy>=1.26,<2.2
pillow>=10.4,<12

# Face detection + recognition (InsightFace + ONNX runtime).
# Default to CPU-only onnxruntime for portability. To enable GPU on
# Linux/Windows with an NVIDIA card:
#   pip uninstall onnxruntime
#   pip install onnxruntime-gpu
# The Recognizer auto-detects CUDAExecutionProvider and switches to GPU.
insightface==0.7.3
onnxruntime==1.20.1

# Gaze detection (Moondream2 via HuggingFace transformers; uses the 2025-01-09
# revision — first one shipping detect_gaze). pyvips is required by that
# revision's image-encoding path (`brew install vips` provides libvips on Mac).
transformers>=4.44,<4.50
einops>=0.8
pyvips>=3.0
# torch comes transitively (pinning minor to prevent accidental downgrade).
# torchvision + timm are required by the gazelle hub repo (fkryan/gazelle).
# The default PyPI wheel is CPU-only on Windows. To enable GPU there:
#   pip uninstall torch torchvision
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# On Linux the default PyPI torch already ships with CUDA. Both gazelle and
# moondream auto-detect CUDA/MPS and switch accordingly.
torch>=2.2
torchvision>=0.17
timm>=0.9

# MediaPipe FaceLandmarker (tasks API) + OpenCV for iris tracking. Combined
# with the InsightFace head-pose signal, this gives a clean "looking at
# camera" verdict: both head frontality and iris centering must agree.
mediapipe==0.10.33
opencv-python>=4.8
