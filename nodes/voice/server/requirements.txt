fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
numpy>=1.26,<2.2
onnxruntime==1.20.1
huggingface-hub==0.27.0
aiosqlite==0.20.0
python-multipart==0.0.20
sounddevice==0.5.1

# Phase 2 engine
faster-whisper==1.1.0
sherpa-onnx==1.12.39

# Optional GPU runtime for ctranslate2 (faster-whisper). Installed on
# Linux/Windows; pip skips them on macOS where the nvidia wheels aren't
# available. Loaded lazily by app/stt.py only when CUDA is detected — safe
# to ship as defaults so any machine with an NVIDIA GPU gets accelerated
# transcription out of the box.
nvidia-cudnn-cu12>=9.0; sys_platform != "darwin"
nvidia-cublas-cu12; sys_platform != "darwin"
